// Placeholder implementation file for Arduino scaffold.
//
// The public headers have been copied into src/include/ so the library
// exposes the API surface. You must port the implementation (.cc files)
// from the ESP-IDF component into this folder and adapt IDF-specific
// calls (esp_event, esp_http_server, nvs_flash, etc.) to Arduino-friendly
// equivalents or provide compatibility wrappers.

#include "wifi_manager.h"

// Minimal stub to avoid empty translation unit when compiling partial ports.
namespace {
  volatile int _arduino_esp_wifi_connect_placeholder = 0;
}
